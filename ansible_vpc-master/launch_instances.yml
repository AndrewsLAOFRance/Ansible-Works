# task file for launch instances with all configuration 
---
    - name: Create and run EC2 instances 
      hosts: localhost
      connection: local
      gather_facts: false

      roles:

        - role: ec2
          tags: run ec2 instances
      